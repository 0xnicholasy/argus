# AXL Round-Trip Latency Log

Appended to by `scripts/measure-latency.ts`. Each entry: timestamp, label (loopback / remote-host), trial count, min/p50/p95/mean, raw samples, peer IDs.

Re-plan trigger (per `.claude/commands/plan.md` P7): if remote-host p95 > 5s, collapse both AXL nodes to a single host for the demo.

---

<!-- entries appended below -->
