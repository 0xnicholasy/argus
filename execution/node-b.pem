# PLACEHOLDER — generate via: axl keygen > execution/node-b.pem
# DISTINCT from signal/node-a.pem (codex CRITICAL — same key = same Yggdrasil pubkey/IPv6 = broken routing).
# This file is .gitignored in production; committed empty as a layout marker.
