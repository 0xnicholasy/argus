# PLACEHOLDER — generate via: axl keygen > signal/node-a.pem
# DISTINCT from execution/node-b.pem (codex CRITICAL — same key = same Yggdrasil pubkey = broken routing).
# This file is .gitignored in production; committed empty as a layout marker.
